[phases.setup]
nixPkgs = [
    "nodejs-18_x",
    "postgresql"
]

[phases.install]
cmds = [
    "cd backend && npm ci --only=production"
]

[phases.build]
cmds = [
    "echo 'Build completed'"
]

[start]
cmd = "cd backend && node server.js"